<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Daraz Admin | OTP Verification</title>
    <style>
      body {
        font-family: 'Segoe UI', Arial, sans-serif;
        background-color: #f8f9fa;
        margin: 0;
        padding: 30px;
        color: #333;
      }

      .email-wrapper {
        display: flex;
        justify-content: center;
        align-items: center;
      }

      .email-container {
        max-width: 550px;
        background: #ffffff;
        border-radius: 10px;
        padding: 40px 30px;
        box-shadow: 0 4px 15px rgba(0,0,0,0.08);
        border-top: 5px solid #ff6f00;
      }

      .header {
        text-align: center;
        margin-bottom: 25px;
      }

      .header h2 {
        color: #ff6f00;
        font-size: 24px;
        margin-bottom: 5px;
      }

      .header p {
        color: #777;
        font-size: 14px;
        margin: 0;
      }

      .otp-box {
        background-color: #f9f9f9;
        border: 2px dashed #ff6f00;
        padding: 15px 0;
        text-align: center;
        margin: 25px 0;
        border-radius: 8px;
      }

      .otp-code {
        font-size: 30px;
        font-weight: bold;
        color: #333;
        letter-spacing: 6px;
      }

      .content p {
        font-size: 15px;
        color: #555;
        line-height: 1.6;
      }

      .footer {
        text-align: center;
        margin-top: 30px;
        font-size: 13px;
        color: #999;
      }

      .footer a {
        color: #ff6f00;
        text-decoration: none;
        font-weight: 600;
      }

      @media (max-width: 600px) {
        .email-container {
          padding: 25px 20px;
        }

        .otp-code {
          font-size: 26px;
        }
      }
    </style>
  </head>
  <body>
    <div class="email-wrapper">
      <div class="email-container">
        <div class="header">
          <h2>🔐 OTP Verification</h2>
          <p>Secure your Daraz Admin Account</p>
        </div>

        <div class="content">
          <p>Hi <strong><%= username %></strong>,</p>
          <p>Please use the following One-Time Password (OTP) to verify your account:</p>

          <div class="otp-box">
            <div class="otp-code"><%= otp %></div>
          </div>

          <p>This OTP will expire in <b>5 minutes</b>. If you didn’t request this, please ignore this email.</p>
        </div>

        <div class="footer">
          <p>© <%= new Date().getFullYear() %> Daraz Admin System</p>
          <p><a href="">Visit Dashboard</a></p>
        </div>
      </div>
    </div>
  </body>
</html>
